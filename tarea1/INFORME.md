# Las llamadas al sistema se ejecutan primeramente editando syscall.h definiendo la nueva llamada al sistema y asignandole un identificador unico para que no haya rrores con otra definicion, luego se edita syscall.c donde se agrega la nueva llamada a la lista de llamadas de sistema, y se agrega la funcion que ejecutara es llamada, es decir, se declara que tipo de funcion sera. Luego se pasa a proc.c donde se identifica la logica de la funcion y se escrie de que manera funcionara la logica. Luego en la carpeta User, se agrega la funcion a utilizar declarando su tipo y los parametros que recibe en user.h, luego en usys.pl, se define la entrada de la funcion, despues de esto se crea el archivo ejecutable donde se llamara a la fucnion anterior asignandole el numero por pantalla y la posible respuesta. Por ultimo, se edita el Makefile agregando la imagen del ejecutable en UPROGS, es decir, se agrega el nuevo programa que se ejecutara cuando compile el qemu, para esto, se debe copiar los UPROGS anteriores y agregarle en el mismo formato el nombre del ejecutable.


# Dificultades: no se clono correctamente el repositorio y no tenia la carpeta mkfs, con lo cual el programa no ejecutaba, para esto, se copio la carpeta faltante desde el repositorio, luego se tuvo problemas ubicando los lugares para agregar las funciones en los archivos correspondientes, y por ultimo se tuvo problemas con el argint definido en syscall.c, ya que, esta funcion es un void que no retorna un parametro, por ende se asume que obtiene correctamente el numero, y con eso se comienza a definir los casos para devolver -1 o no